[tool.poetry]
name = "oz-django-1"
version = "0.1.0"
description = ""
authors = ["gunung-kim"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
django = "^6.0.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
